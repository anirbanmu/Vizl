function Vector2d(x, y) {
    this.x = x;
    this.y = y;
}

function Vector3d(x, y ,z) {
    this.x = y;
    this.y = y;
    this.z = z;
}

function Angle(a) {
    this.angle = a;
    this.cos = Math.cos(a);
    this.sin = Math.sin(a);
}
